<script>
  CCR_DATA = {
      <% [CareReason, CareType, Language, LanguageProvider, Neighborhood, State, ZipCode].each do |m| %>
        "<%= m.name.underscore.pluralize %>": <%= m.all.index_by(&:id).to_json.html_safe%>,
      <% end %>
  }
</script>



<%
  # [CareReason, CareType, Language, LanguageProvider, License, Neighborhood, State, ZipCode]
%>
